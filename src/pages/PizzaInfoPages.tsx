import React from "react";
import {PizzaInformation} from "../components";

const PizzaInfoPages: React.FC = () => {
	return (
		<>
			<PizzaInformation/>
		</>
	);
};

export default PizzaInfoPages;
